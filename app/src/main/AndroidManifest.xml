<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.brioche.bierix">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_bierix144"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bierix">
        <activity android:name=".Profil"/>
        <activity android:name=".AjoutPanier" />
        <activity android:name=".ActivityLike" />


        <activity android:name=".Search"></activity>

        <activity android:name=".Panier" />
        <activity android:name=".Redirection" />
        <activity android:name=".Description_biere" />
        <activity android:name=".creation_account" />
        <activity android:name=".login_class" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>